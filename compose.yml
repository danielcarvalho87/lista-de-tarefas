version: '3.8'

services:
  react-app:
    container_name: lista-de-tarefas
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "20333:3000"
    volumes:
      - ./:/  # Mapeia o diretório atual para a raiz do container
      - /node_modules  # Isola node_modules na raiz
    environment:
      - NODE_ENV=development
